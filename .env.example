HOST=0.0.0.0
PORT=3000

VITE_API_PROXY=http://localhost:3000
# Optional UI label shown in the desktop-style status bar (for example: Production, Test, UAT)
APP_ENV_LABEL=Test

# CRA desktop client download (used by /api/client/download* endpoints)
# Source mode: github (default) or local
# CRA_CLIENT_RELEASE_SOURCE=github
# CRA_CLIENT_RELEASE_ALLOW_LOCAL_FALLBACK=true
# CRA_CLIENT_RELEASE_CACHE_SECONDS=300
# CRA_CLIENT_RELEASE_NEGATIVE_CACHE_SECONDS=30
#
# GitHub source configuration
# CRA_CLIENT_GITHUB_OWNER=RenoMO-lab
# CRA_CLIENT_GITHUB_REPO=CRA_client
# CRA_CLIENT_GITHUB_ASSET_PATTERN=windows-x64.exe
# CRA_CLIENT_GITHUB_TOKEN=
#
# Local fallback (used when source=local, or when github fails and fallback is enabled)
# CRA_CLIENT_INSTALLER_PATH=C:\CRA_Local_Main\artifacts\CRA-Client-latest.exe
# CRA_CLIENT_INSTALLER_NAME=CRA-Client-latest.exe
# CRA_CLIENT_INSTALLER_VERSION=v0.1.19
# CRA_CLIENT_INSTALLER_SHA256=

# Option A (recommended): single URL
# DATABASE_URL=postgres://cra_app:password@localhost:5432/cra_local

# Option B: discrete connection vars (used if DATABASE_URL is not set)
PGHOST=localhost
PGPORT=5432
PGDATABASE=cra_local
PGUSER=cra_app
PGPASSWORD=your_postgres_password

# Optional: increase if users attach multiple large files in one save.
JSON_BODY_LIMIT=50mb

# Session auth (server-side sessions)
SESSION_COOKIE_NAME=cra_sid
SESSION_TTL_HOURS=24
# SESSION_COOKIE_SECURE_MODE=auto
# SESSION_COOKIE_SECURE=true

# Optional: secret used to encrypt backup credentials stored in DB.
# If omitted, app uses a host-derived fallback key (acceptable for test VM, not ideal for production).
# BACKUP_CREDENTIALS_SECRET=change_this_to_a_long_random_string

# Bootstrap admin (used only when app_users is empty)
BOOTSTRAP_ADMIN_NAME=Admin
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change_me
